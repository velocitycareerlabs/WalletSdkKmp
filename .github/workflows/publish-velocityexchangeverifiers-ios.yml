name: Publish velocityexchangeverifiers iOS

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Configuration environment'
        required: true
        type: choice
        options: [rc, prod]

env:
  JAVA_VERSION: "17"

permissions:
  contents: write
  packages: write

concurrency:
  group: kmp-ios-publish-${{ github.ref }}-${{ github.event.inputs.environment }}
  cancel-in-progress: true

jobs:
  build-and-publish-ios-sdk:
    runs-on: macos-15
    env:
      XC_VERSION: "16.3"
      FRAMEWORKS_REPO: velocitycareerlabs/KmpIosFrameworks
      SPECS_REPO: velocitycareerlabs/KmpIosSpecs
      PAT_TOKEN: ${{ secrets.VCL_RW_ACCESS_TOKEN }}

    steps:
      - name: 🧾 Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_${{ env.XC_VERSION }}.app

      - name: ☕ Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}

      - name: 🛠️ Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Print tool versions
        run: |
          java -version
          ./gradlew --version
          pod --version || true

      - name: 📝 Read base version from gradle.properties
        id: ver
        shell: bash
        run: |
          set -euo pipefail
          VERSION=$(grep '^PUBLISH_VERSION=' gradle.properties | cut -d'=' -f2 | tr -d '[:space:]')
          if [ -z "$VERSION" ]; then echo "PUBLISH_VERSION missing in gradle.properties"; exit 1; fi
          echo "base_version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Found base version: $VERSION"

      - name: 🔧 Compute EFFECTIVE_VERSION
        id: eff
        shell: bash
        run: |
          set -euo pipefail
          BASE="${{ steps.ver.outputs.base_version }}"
          if [ "${{ github.event.inputs.environment }}" = "rc" ]; then
            EFFECTIVE="${BASE}-rc"; PRERELEASE=true
          else
            EFFECTIVE="${BASE}"; PRERELEASE=false
          fi
          echo "effective=$EFFECTIVE" >> "$GITHUB_OUTPUT"
          echo "EFFECTIVE_VERSION=$EFFECTIVE" >> "$GITHUB_ENV"
          echo "IS_PRERELEASE=$PRERELEASE" >> "$GITHUB_ENV"
          echo "Effective version: $EFFECTIVE (prerelease=$PRERELEASE)"

      - name: 🏗️ Build XCFramework via KMP
        shell: bash
        run: |
          set -euo pipefail
          ./gradlew --no-daemon --warning-mode=all \
            :velocityexchangeverifiers:clean \
            :velocityexchangeverifiers:assembleIos \
            -PprojectVersion=${{ steps.ver.outputs.base_version }} \
            -Pprerelease=${{ github.event.inputs.environment == 'rc' }} \
            --stacktrace

      - name: 🧾 Generate podspec via KMP
        shell: bash
        run: |
          set -euo pipefail
          ./gradlew --no-daemon :velocityexchangeverifiers:podspec \
            -PprojectVersion=${{ steps.ver.outputs.base_version }} \
            -Pprerelease=${{ github.event.inputs.environment == 'rc' }}

      - name: 🔎 Locate outputs & validate podspec version
        id: locate
        shell: bash
        run: |
          set -euo pipefail
          # XCFramework (release)
          XCF="velocityexchangeverifiers/build/XCFrameworks/release/velocityexchangeverifiers.xcframework"
          test -d "$XCF" || { echo "XCFramework not found: $XCF"; exit 1; }

          # Prefer module-root podspec; else search under build/
          ROOT_PODSPEC="velocityexchangeverifiers/velocityexchangeverifiers.podspec"
          if [[ -f "$ROOT_PODSPEC" ]]; then
            PODSPEC="$ROOT_PODSPEC"
          else
            PODSPEC=$(find velocityexchangeverifiers/build -maxdepth 6 -type f -name "*.podspec" | head -n1 || true)
          fi

          if [[ -z "${PODSPEC:-}" || ! -f "$PODSPEC" ]]; then
            echo "Podspec not found in module root or under build/. Tree (trimmed):"
            find velocityexchangeverifiers -maxdepth 4 -print
            exit 1
          fi

          echo "Found podspec at: $PODSPEC"
          echo "xcframework=$XCF" >> "$GITHUB_OUTPUT"
          echo "podspec=$PODSPEC" >> "$GITHUB_OUTPUT"

          # Validate version matches EFFECTIVE_VERSION
          if ! grep -E "spec\.version\s*=\s*['\"]${EFFECTIVE_VERSION}['\"]" "$PODSPEC" >/dev/null; then
            echo "❌ Podspec version mismatch (expected ${EFFECTIVE_VERSION})"
            sed -n '1,200p' "$PODSPEC"
            exit 1
          fi
          echo "✅ Podspec version OK"

      - name: 📦 Zip XCFramework
        id: zipit
        shell: bash
        run: |
          set -euo pipefail
          OUT="velocityexchangeverifiers-${{ env.EFFECTIVE_VERSION }}.xcframework.zip"
          ditto -c -k --sequesterRsrc --keepParent "${{ steps.locate.outputs.xcframework }}" "$OUT"
          echo "zip=$OUT" >> "$GITHUB_OUTPUT"
          ls -lh "$OUT"

      - name: 🔍 Sanity check zip contents
        shell: bash
        run: |
          set -euo pipefail
          ZIP="${{ steps.zipit.outputs.zip }}"
          unzip -l "$ZIP" | sed -n '1,60p'
          unzip -l "$ZIP" | grep -q "velocityexchangeverifiers\.xcframework/" || {
            echo "❌ zip does not contain velocityexchangeverifiers.xcframework at the expected path"; exit 1; }
          echo "✅ zip contents OK"

      - name: 🔑 Git identity
        shell: bash
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'

      # --- Publish FRAMEWORK to velocitycareerlabs/KmpIosFrameworks (main) ---
      - name: ⬇️ Clone Frameworks repo (main)
        shell: bash
        run: |
          set -euo pipefail
          git clone "https://${{ env.PAT_TOKEN }}@github.com/${{ env.FRAMEWORKS_REPO }}" frameworks-repo
          cd frameworks-repo
          git pull --rebase origin main || true

      - name: 🗂️ Commit framework zip to Frameworks repo
        working-directory: frameworks-repo
        shell: bash
        run: |
          set -euo pipefail
          VER="${{ env.EFFECTIVE_VERSION }}"
          DEST_DIR="VelocityExchangeVerifiers/${VER}"
          mkdir -p "$DEST_DIR"
          cp -f "../${{ steps.zipit.outputs.zip }}" "$DEST_DIR/"
          git add -A
          if git diff --cached --quiet; then
            echo "No binary changes to commit"
          else
            git commit -m "VelocityExchangeVerifiers ${VER} (xcframework zip)"
            git push origin main
          fi

      # --- Publish SPEC to velocitycareerlabs/KmpIosSpecs (main) ---
      - name: ⬇️ Clone Specs repo (main)
        shell: bash
        run: |
          set -euo pipefail
          git clone "https://${{ env.PAT_TOKEN }}@github.com/${{ env.SPECS_REPO }}" specs-repo
          cd specs-repo
          git pull --rebase origin main || true

      - name: 🧩 Commit podspec to Specs repo
        working-directory: specs-repo
        shell: bash
        run: |
          set -euo pipefail
          VER="${{ env.EFFECTIVE_VERSION }}"
          SPECS_DIR="VelocityExchangeVerifiers/${VER}"
          mkdir -p "$SPECS_DIR"

          # Copy generated podspec
          cp -f "../${{ steps.locate.outputs.podspec }}" "$SPECS_DIR/VelocityExchangeVerifiers.podspec"
          PODSPEC="$SPECS_DIR/VelocityExchangeVerifiers.podspec"

          # Normalize fields using BSD sed (macOS runner) - in place with backup ''.
          # Name
          sed -E -i '' 's/^\s*(s|spec)\.name\s*=.*/  s.name = "VelocityExchangeVerifiers"/' "$PODSPEC"

          # Source -> raw ZIP in KmpIosFrameworks
          ZIP_URL="https://raw.githubusercontent.com/${{ env.FRAMEWORKS_REPO }}/main/VelocityExchangeVerifiers/${VER}/velocityexchangeverifiers-${VER}.xcframework.zip"
          sed -E -i '' "s#^\\s*(s|spec)\\.source\\s*=.*#  s.source = { :http => \"${ZIP_URL}\" }#" "$PODSPEC"

          # Vendored frameworks
          sed -E -i '' "s#^\\s*(s|spec)\\.vendored_frameworks\\s*=.*#  s.vendored_frameworks = \"velocityexchangeverifiers.xcframework\"#" "$PODSPEC"

          # Version
          sed -E -i '' "s#^\\s*(s|spec)\\.version\\s*=.*#  s.version = \"${VER}\"#" "$PODSPEC"

          git add -A
          if git diff --cached --quiet; then
            echo "No spec changes to commit"
          else
            git commit -m "Specs VelocityExchangeVerifiers ${VER}"
            git push origin main
          fi

      - name: ⬆️ Upload artifacts (debug)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ios-${{ env.EFFECTIVE_VERSION }}
          path: ${{ steps.zipit.outputs.zip }}
